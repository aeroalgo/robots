# План рефакторинга модуля discovery

## Дата создания
2025-01-27

## Обзор
Детальный план рефакторинга модуля `src/discovery/` с приоритизацией проблем и решениями.

## ПРИОРИТЕТЫ РЕФАКТОРИНГА

### 🔴 Приоритет 1: Критические исправления

#### 1.1 Добавление тестов (TDD подход)
**Проблема**: Недостаточное покрытие тестами (только 3 теста)

**Решение**:
- Написать unit-тесты для всех модулей ПЕРЕД рефакторингом
- Покрыть основные сценарии, граничные случаи и обработку ошибок
- Добавить тесты для `StrategyConverter::candidate_to_definition()`
- Добавить тесты для `ParameterExtractor`
- Добавить тесты для `ConditionBuilder`
- Добавить тесты для `IndicatorInfoCollector`

**Шаги реализации**:
1. Добавить тесты для `StrategyConverter`:
   - Тест конвертации базового кандидата
   - Тест конвертации с вложенными индикаторами
   - Тест конвертации с разными типами условий
   - Тест конвертации с stop/take handlers
   - Тест обработки ошибок
2. Добавить тесты для `ParameterExtractor`:
   - Тест извлечения параметров индикаторов
   - Тест извлечения параметров условий
   - Тест извлечения параметров stop handlers
   - Тест извлечения параметров take handlers
   - Тест граничных случаев
3. Добавить тесты для `ConditionBuilder`:
   - Тест создания bindings для разных типов условий
   - Тест обработки таймфреймов
   - Тест обработки ошибок
4. Добавить тесты для `IndicatorInfoCollector`:
   - Тест сбора информации из реестра
   - Тест преобразования категорий
   - Тест определения параметров

**Ожидаемый результат**: Покрытие тестами 85%+, все тесты проходят

#### 1.2 Устранение дублирования кода
**Проблема**: Сильное дублирование в `strategy_converter/`

**Вариант 1: Создание общих helper методов**
**Плюсы**:
- Простота реализации
- Легко понять и поддерживать
- Минимальные изменения в существующем коде

**Минусы**:
- Все еще есть некоторая дублированная логика
- Может быть не оптимально для всех случаев

**Вариант 2: Использование trait'ов для конвертации**
**Плюсы**:
- Хорошая архитектура
- Легко расширять
- Соблюдение OCP

**Минусы**:
- Требует больше рефакторинга
- Может быть избыточно для простых случаев

**Вариант 3: Использование макросов**
**Плюсы**:
- Полное устранение дублирования
- Генерация кода на этапе компиляции

**Минусы**:
- Сложнее отлаживать
- Может быть менее читаемым

**Рекомендуемый подход**: Комбинация вариантов 1 и 2
- Создать общие helper методы для создания `DataSeriesSource` и определения таймфреймов
- Создать trait `ConditionConverter` для разных типов условий
- Использовать helper методы для повторяющейся логики

**Шаги реализации**:
1. Создать модуль `strategy_converter/helpers.rs` с общими методами:
   ```rust
   pub fn create_indicator_source(alias: &str, timeframe: Option<TimeFrame>, ...) -> DataSeriesSource
   pub fn create_price_source(price_field: PriceField, timeframe: Option<TimeFrame>) -> DataSeriesSource
   pub fn determine_timeframe_for_indicator(...) -> Option<TimeFrame>
   pub fn extract_percent_param(...) -> Result<f32, StrategyConversionError>
   pub fn extract_period_param(...) -> Result<f32, StrategyConversionError>
   ```
2. Создать trait `ConditionConverter`:
   ```rust
   trait ConditionConverter {
       fn convert_to_input(
           condition: &ConditionInfo,
           candidate: &StrategyCandidate,
           indicator_bindings: &[IndicatorBindingSpec],
       ) -> Result<ConditionInputSpec, StrategyConversionError>;
   }
   ```
3. Реализовать конвертеры для каждого типа условия:
   - `IndicatorPriceConverter`
   - `IndicatorIndicatorConverter`
   - `TrendConditionConverter`
   - `IndicatorConstantConverter`
4. Рефакторить `ConditionBuilder::create_condition_input()` для использования trait'ов
5. Рефакторить `ParameterExtractor` для использования общих helper методов

**Ожидаемый результат**: Устранение 70%+ дублирования кода

#### 1.3 Разделение ответственности StrategyConverter
**Проблема**: `StrategyConverter` имеет слишком много ответственности (524 строки)

**Решение**: Разделить на специализированные модули

**Структура**:
```
strategy_converter/
  main.rs              # StrategyConverter (координатор)
  metadata_builder.rs  # Создание метаданных
  indicator_builder.rs # Создание indicator bindings
  condition_builder.rs # Уже есть, но улучшить
  handler_builder.rs   # Создание stop/take handlers
  rule_builder.rs      # Создание entry/exit rules
  helpers.rs           # Общие helper методы
  mod.rs               # Объявление модулей
```

**Шаги реализации**:
1. Создать `metadata_builder.rs` с методом `create_metadata()`
2. Создать `indicator_builder.rs` с методом `create_indicator_bindings()`
3. Создать `handler_builder.rs` с методами `create_stop_handlers()` и `create_take_handlers()`
4. Создать `rule_builder.rs` с методами `create_entry_rules()` и `create_exit_rules()`
5. Обновить `main.rs` для использования специализированных модулей
6. Обновить тесты

**Ожидаемый результат**: Соблюдение SRP, лучшая модульность

#### 1.4 Разбиение длинных функций
**Проблема**: Функции содержат 70-200+ строк, высокая сложность

**Решение**: Разбить на меньшие функции

**Для `create_condition_input()`**:
- `create_indicator_price_input()` - для indicator_price
- `create_indicator_indicator_input()` - для indicator_indicator
- `create_trend_condition_input()` - для trend_condition
- `create_indicator_constant_input()` - для indicator_constant
- `determine_indicator_timeframe()` - определение таймфрейма
- `determine_price_timeframe()` - определение таймфрейма для цены

**Для `extract_stop_handler_parameters()`**:
- `extract_indicator_params_for_stop()` - параметры индикатора
- `extract_handler_params_for_stop()` - параметры обработчика
- `determine_indicator_category()` - определение категории

**Для `candidate_to_definition()`**:
- Уже разделено на методы, но можно улучшить

**Шаги реализации**:
1. Разбить `create_condition_input()` на методы по типам условий
2. Разбить `extract_stop_handler_parameters()` на методы
3. Разбить `extract_take_handler_parameters()` на методы
4. Обновить тесты

**Ожидаемый результат**: Снижение сложности до 5-7, длина функций < 50 строк

### 🟠 Приоритет 2: Архитектурные улучшения

#### 2.1 Удаление или объединение почти пустых файлов
**Проблема**: `indicator.rs`, `timeframe.rs`, `condition.rs` почти пустые (7 строк каждый)

**Решение**: Удалить или объединить в один файл `generators.rs`

**Шаги реализации**:
1. Проверить использование этих структур в коде
2. Если не используются - удалить
3. Если используются - объединить в `generators.rs` или оставить как есть с документацией

**Ожидаемый результат**: Улучшенная организация кода

#### 2.2 Реализация Strategy Pattern для конвертации условий
**Проблема**: Нарушение OCP - жесткая логика для разных типов условий

**Решение**: Использовать Strategy pattern через trait'ы

**Шаги реализации**:
1. Создать trait `ConditionConverter` (как описано в 1.2)
2. Реализовать конвертеры для каждого типа условия
3. Обновить `ConditionBuilder` для использования trait'ов
4. Обновить тесты

**Ожидаемый результат**: Соблюдение OCP, легкость добавления новых типов условий

#### 2.3 Разделение ParameterExtractor
**Проблема**: `ParameterExtractor` содержит логику для разных типов параметров (457 строк)

**Решение**: Разделить на специализированные модули

**Структура**:
```
strategy_converter/
  parameter_extractor/
    mod.rs              # ParameterExtractor (координатор)
    indicator.rs        # Извлечение параметров индикаторов
    condition.rs        # Извлечение параметров условий
    handler.rs          # Извлечение параметров handlers
    helpers.rs          # Общие helper методы
```

**Шаги реализации**:
1. Создать `parameter_extractor/indicator.rs`
2. Создать `parameter_extractor/condition.rs`
3. Создать `parameter_extractor/handler.rs`
4. Создать `parameter_extractor/helpers.rs` с общими методами
5. Обновить `mod.rs` для координации
6. Обновить тесты

**Ожидаемый результат**: Соблюдение SRP, лучшая модульность

#### 2.4 Добавление документации
**Проблема**: Отсутствие rustdoc комментариев

**Решение**: Добавить документацию для всех публичных API

**Шаги реализации**:
1. Добавить rustdoc для всех публичных структур и методов
2. Добавить примеры использования в документации
3. Добавить описание архитектуры модуля
4. Добавить диаграммы потоков данных

**Ожидаемый результат**: Покрытие документацией 80%+

### 🟡 Приоритет 3: Улучшения качества кода

#### 3.1 Улучшение обработки ошибок
**Проблема**: `unwrap_or_else()` может скрыть реальные проблемы

**Решение**: Заменить на `expect()` с понятными сообщениями или явную обработку ошибок

**Шаги реализации**:
1. Заменить все `unwrap_or_else()` на `expect()` с описательными сообщениями
2. Улучшить контекст в сообщениях об ошибках
3. Добавить информацию о том, какая часть конвертации вызвала ошибку

**Ожидаемый результат**: Лучшая отладка, понятные сообщения об ошибках

#### 3.2 Оптимизация аллокаций
**Проблема**: Избыточные аллокации в горячих путях

**Решение**:
- Использовать ссылки вместо клонирования где возможно
- Использовать `Arc<str>` для переиспользования строк
- Использовать `SmallVec` для небольших векторов
- Избегать создания временных обработчиков

**Шаги реализации**:
1. Заменить `String::clone()` на ссылки где возможно
2. Использовать `Arc<str>` для часто используемых строк
3. Использовать `SmallVec` для небольших векторов параметров
4. Оптимизировать создание временных обработчиков в `ParameterExtractor`

**Ожидаемый результат**: Снижение аллокаций на 20-30%

#### 3.3 Централизация валидации
**Проблема**: Валидация выполняется в разных местах

**Решение**: Создать централизованный валидатор

**Шаги реализации**:
1. Создать модуль `strategy_converter/validator.rs`
2. Реализовать методы валидации для разных частей кандидата
3. Использовать валидатор в `StrategyConverter`

**Ожидаемый результат**: Централизованная валидация, лучшая обработка ошибок

### 🟢 Приоритет 4: Дополнительные улучшения

#### 4.1 Кэширование результатов (опционально)
**Проблема**: Нет кэширования результатов конвертации

**Решение**: Добавить мемоизацию для часто используемых преобразований

**Примечание**: Это может усложнить код и требует дополнительного тестирования

#### 4.2 Ленивые вычисления (опционально)
**Проблема**: Все вычисляется сразу

**Решение**: Использовать ленивые вычисления для некоторых частей

**Примечание**: Это может усложнить код

## ПЛАН ВЫПОЛНЕНИЯ

### Фаза 1: Подготовка (TDD)
1. ✅ Написать тесты для всех модулей ПЕРЕД рефакторингом
2. ✅ Запустить тесты - они должны проходить
3. ✅ Задокументировать текущее поведение

### Фаза 2: Критические исправления
1. Устранение дублирования кода (1.2)
2. Разделение ответственности StrategyConverter (1.3)
3. Разбиение длинных функций (1.4)

### Фаза 3: Архитектурные улучшения
1. Удаление или объединение почти пустых файлов (2.1)
2. Реализация Strategy Pattern для конвертации условий (2.2)
3. Разделение ParameterExtractor (2.3)
4. Добавление документации (2.4)

### Фаза 4: Улучшения качества
1. Улучшение обработки ошибок (3.1)
2. Оптимизация аллокаций (3.2)
3. Централизация валидации (3.3)

### Фаза 5: Тестирование и валидация
1. Запустить все тесты
2. Проверить покрытие тестами (должно быть 85%+)
3. Проверить производительность (бенчмарки)
4. Проверить документацию

## МЕТРИКИ УСПЕХА

### Критерии завершения
- ✅ Покрытие тестами: 85%+
- ✅ Циклическая сложность: < 7
- ✅ Длина функций: < 50 строк
- ✅ Дублирование кода: < 10%
- ✅ Документация: 80%+
- ✅ Все тесты проходят
- ✅ Производительность улучшена на 10-20%

### Ожидаемые улучшения

| Метрика | До | После | Улучшение |
|---------|-----|-------|-----------|
| Покрытие тестами | 5% | 85%+ | +80% |
| Циклическая сложность | 10-15 | 5-7 | -50% |
| Длина функций | 70-200+ | 30-50 | -60% |
| Дублирование кода | Высокое | < 10% | Устранено |
| Документация | 0% | 80%+ | +80% |
| Производительность | Базовый | Оптимизированный | +10-20% |
| Соблюдение SOLID | Частичное | Полное | Улучшено |

## РИСКИ И МИТИГАЦИЯ

### Риски
1. **Регрессии**: Изменения могут сломать существующий функционал
   - **Митигация**: TDD подход, все тесты должны проходить после каждого изменения

2. **Производительность**: Оптимизации могут ухудшить производительность
   - **Митигация**: Бенчмарки до и после, профилирование

3. **Сложность**: Рефакторинг может усложнить код
   - **Митигация**: Постепенные изменения, ревью кода

4. **Время**: Рефакторинг может занять много времени
   - **Митигация**: Приоритизация, фазовый подход

## ЗАВИСИМОСТИ

### Внутренние зависимости
- Модуль `indicators` (для `IndicatorRegistry`)
- Модуль `strategy` (для `StrategyDefinition`)
- Модуль `risk` (для stop/take handlers)
- Модуль `optimization` (для `ConditionId`)

### Внешние зависимости
- `serde` (для сериализации)
- `thiserror` (для типизированных ошибок)
- `chrono` (для временных меток)

## СЛЕДУЮЩИЕ ШАГИ

После завершения рефакторинга:
1. Интеграция с модулем `optimization` (генетический алгоритм)
2. Добавление новых типов условий
3. Оптимизация для больших объемов данных
4. Добавление метрик производительности
