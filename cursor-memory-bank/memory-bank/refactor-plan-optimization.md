# План рефакторинга модуля optimization

## Обзор
Детальный план рефакторинга модуля `src/optimization/` с приоритетами и решениями.

## Приоритеты

### 🔴 Приоритет 1: Критические исправления

#### 1.1 Добавление тестов (TDD подход)

**Проблема**: Отсутствие тестов для большинства модулей (только 2 файла имеют тесты)

**Решение**: Добавить unit-тесты для всех модулей перед рефакторингом

**Варианты**:
1. **Вариант 1**: Добавить тесты в каждый файл в модуле `#[cfg(test)]`
   - Плюсы: Простота, близость к коду
   - Минусы: Большие файлы станут еще больше
2. **Вариант 2**: Создать отдельные тестовые файлы в `tests/optimization/`
   - Плюсы: Разделение кода и тестов
   - Минусы: Сложнее поддерживать синхронизацию
3. **Вариант 3**: Комбинированный подход (unit-тесты в файлах, integration-тесты в `tests/`)
   - Плюсы: Гибкость, лучшая организация
   - Минусы: Требует планирования

**Рекомендуемый подход**: Вариант 3 - комбинированный подход

**Руководство по реализации**:
1. Для каждого модуля создать unit-тесты в `#[cfg(test)] mod tests`
2. Для интеграционных тестов создать `tests/optimization/`
3. Покрыть тестами:
   - Все публичные методы
   - Граничные случаи
   - Обработку ошибок
   - Основные сценарии использования

**Модули для тестирования**:
- `genetic.rs`: тесты кроссовера, мутации, отбора
- `population.rs`: тесты селекции, мутации параметров
- `fitness.rs`: тесты расчета fitness, проверки порогов
- `evaluator.rs`: тесты оценки стратегий, кэширования
- `candidate_builder.rs`: тесты построения кандидатов, применения правил
- `condition_builder.rs`: тесты генерации условий
- `initial_population.rs`: тесты генерации начальной популяции
- `island.rs`, `migration.rs`, `evolution.rs`, `fresh_blood.rs`: тесты соответствующих операций

#### 1.2 Разбиение больших файлов

**Проблема**: Очень длинные файлы (1986, 1422, 1381, 1205 строк)

**Решение**: Разбить большие файлы на меньшие модули

**Варианты**:
1. **Вариант 1**: Разбить по функциональности (рекомендуется)
   - Плюсы: Логическое разделение, соблюдение SRP
   - Минусы: Требует рефакторинга зависимостей
2. **Вариант 2**: Разбить по типам операций
   - Плюсы: Простота навигации
   - Минусы: Может нарушить логическую связность

**Рекомендуемый подход**: Вариант 1 - разбиение по функциональности

**Руководство по реализации**:

**1. `candidate_builder.rs` (1986 строк) → разделить на**:
- `candidate_builder/mod.rs` - основной интерфейс
- `candidate_builder/phase_builder.rs` - построение фаз
- `candidate_builder/rules_applier.rs` - применение правил
- `candidate_builder/validator.rs` - валидация кандидатов
- `candidate_builder/helpers.rs` - вспомогательные функции

**2. `genetic.rs` (1422 строки) → разделить на**:
- `genetic/mod.rs` - основной интерфейс
- `genetic/crossover.rs` - операции кроссовера
- `genetic/mutation.rs` - операции мутации
- `genetic/selection.rs` - отбор особей
- `genetic/helpers.rs` - вспомогательные функции

**3. `initial_population.rs` (1381 строка) → разделить на**:
- `initial_population/mod.rs` - основной интерфейс
- `initial_population/candidate_generator.rs` - генерация кандидатов
- `initial_population/parameter_generator.rs` - генерация параметров
- `initial_population/evaluator.rs` - оценка кандидатов
- `initial_population/selector.rs` - отбор с разнообразием

**4. `condition_builder.rs` (1205 строк) → разделить на**:
- `condition_builder/mod.rs` - основной интерфейс
- `condition_builder/generators.rs` - генераторы условий по типам
- `condition_builder/validator.rs` - валидация условий
- `condition_builder/helpers.rs` - вспомогательные функции

#### 1.3 Устранение избыточных клонирований

**Проблема**: Избыточные клонирования в горячих путях

**Решение**: Использовать ссылки вместо клонирования где возможно

**Варианты**:
1. **Вариант 1**: Заменить клонирования на ссылки
   - Плюсы: Улучшение производительности
   - Минусы: Требует изменения сигнатур функций
2. **Вариант 2**: Использовать `Arc` для разделяемых данных
   - Плюсы: Минимальные изменения кода
   - Минусы: Overhead от Arc

**Рекомендуемый подход**: Вариант 1 - использовать ссылки где возможно, `Arc` только для параллельного доступа

**Руководство по реализации**:
1. В `evolve_generation()`: использовать `&StrategyCandidate` вместо клонирования
2. В `crossover_structure_hybrid()`: использовать ссылки на родителей
3. В `create_individual()`: клонировать только при необходимости
4. Использовать `Arc<StrategyCandidate>` только для параллельной обработки

#### 1.4 Снижение циклической сложности

**Проблема**: Функции с высокой циклической сложностью (>15)

**Решение**: Разбить длинные функции на меньшие

**Варианты**:
1. **Вариант 1**: Извлечь методы
   - Плюсы: Улучшение читаемости, тестируемости
   - Минусы: Может увеличить количество методов
2. **Вариант 2**: Использовать паттерн Strategy
   - Плюсы: Гибкость, расширяемость
   - Минусы: Overhead от диспетчеризации

**Рекомендуемый подход**: Вариант 1 - извлечь методы, использовать Strategy только для вариативной логики

**Руководство по реализации**:
1. `CandidateBuilder::build_condition()`: разбить на методы по типам условий
2. `GeneticAlgorithmV3::mutate_structure()`: разбить на методы по типам мутаций
3. `InitialPopulationGenerator::generate_random_parameters()`: разбить на методы по типам параметров

### 🟠 Приоритет 2: Архитектурные улучшения

#### 2.1 Устранение дублирования кода

**Проблема**: Сильное дублирование между модулями

**Решение**: Создать общие helper модули

**Варианты**:
1. **Вариант 1**: Создать `optimization/helpers/` модуль
   - Плюсы: Централизация общей логики
   - Минусы: Может стать "god object"
2. **Вариант 2**: Создать специализированные helper модули
   - Плюсы: Лучшая организация, соблюдение SRP
   - Минусы: Больше файлов

**Рекомендуемый подход**: Вариант 2 - специализированные helper модули

**Руководство по реализации**:
1. Создать `optimization/helpers/condition_helpers.rs`:
   - `check_conflicting_operators()`
   - `is_duplicate_condition()`
   - `can_compare_indicators()`
2. Создать `optimization/helpers/parameter_helpers.rs`:
   - `generate_indicator_parameters()`
   - `generate_condition_parameters()`
   - `generate_stop_handler_parameters()`
3. Создать `optimization/helpers/signature_helpers.rs`:
   - `get_strategy_signature()`
   - `create_hypothesis_signature()`

#### 2.2 Оптимизация алгоритмов

**Проблема**: O(n²) операции в критических путях

**Решение**: Оптимизировать алгоритмы до O(n log n) или O(n)

**Варианты**:
1. **Вариант 1**: Использовать `HashSet` вместо `Vec` для проверки уникальности
   - Плюсы: O(1) вместо O(n) для проверки
   - Минусы: Требует `Hash` для типов
2. **Вариант 2**: Использовать `BTreeSet` для сортированных коллекций
   - Плюсы: O(log n) для операций, автоматическая сортировка
   - Минусы: Требует `Ord` для типов

**Рекомендуемый подход**: Вариант 1 - использовать `HashSet` для проверки уникальности

**Руководство по реализации**:
1. В `select_with_diversity()`: использовать `HashSet` для группировки
2. В `crossover_conditions_hybrid()`: использовать `HashSet` для проверки уникальности
3. В `detect_duplicates()`: использовать `HashSet` для отслеживания увиденных

#### 2.3 Разделение ответственности

**Проблема**: Нарушение SRP в больших структурах

**Решение**: Разделить структуры на специализированные компоненты

**Варианты**:
1. **Вариант 1**: Использовать композицию
   - Плюсы: Гибкость, тестируемость
   - Минусы: Может увеличить количество объектов
2. **Вариант 2**: Использовать трейты для абстракции
   - Плюсы: Расширяемость, полиморфизм
   - Минусы: Overhead от диспетчеризации

**Рекомендуемый подход**: Вариант 1 - использовать композицию, трейты только для вариативной логики

**Руководство по реализации**:
1. `GeneticAlgorithmV3`: разделить на:
   - `GeneticAlgorithm` - основной интерфейс
   - `CrossoverEngine` - операции кроссовера
   - `MutationEngine` - операции мутации
   - `SelectionEngine` - отбор особей
2. `CandidateBuilder`: разделить на:
   - `CandidateBuilder` - основной интерфейс
   - `PhaseBuilder` - построение фаз
   - `RulesApplier` - применение правил
   - `CandidateValidator` - валидация

### 🟡 Приоритет 3: Улучшения качества кода

#### 3.1 Оптимизация структур данных

**Проблема**: Неоптимальные структуры данных

**Решение**: Использовать оптимальные структуры данных

**Руководство по реализации**:
1. Заменить `Vec` на `HashSet` для проверки уникальности
2. Использовать `BTreeMap` для сортированных коллекций
3. Использовать `SmallVec` для небольших векторов

#### 3.2 Добавление документации

**Проблема**: Отсутствие rustdoc комментариев

**Решение**: Добавить rustdoc комментарии для всех публичных API

**Руководство по реализации**:
1. Добавить документацию для всех публичных структур и методов
2. Добавить примеры использования
3. Добавить заметки о производительности где необходимо

## План выполнения

### Фаза 1: Подготовка (TDD)
1. Добавить unit-тесты для всех модулей
2. Запустить тесты - они должны упасть (красная фаза TDD)
3. Задокументировать текущее поведение

### Фаза 2: Критические исправления
1. Разбить большие файлы на модули
2. Устранить избыточные клонирования
3. Снизить циклическую сложность
4. Запустить тесты - они должны пройти (зеленая фаза TDD)

### Фаза 3: Архитектурные улучшения
1. Устранить дублирование кода
2. Оптимизировать алгоритмы
3. Разделить ответственность
4. Запустить тесты - убедиться что все проходят

### Фаза 4: Улучшения качества
1. Оптимизировать структуры данных
2. Добавить документацию
3. Финальное тестирование

## Ожидаемые результаты

| Метрика | До | После | Улучшение |
|---------|-----|-------|-----------|
| Покрытие тестами | ~5% | 85%+ | +80% |
| Максимальная длина файла | 1986 строк | < 500 строк | -75% |
| Максимальная длина функции | ~500 строк | < 50 строк | -90% |
| Максимальная циклическая сложность | ~30 | < 7 | -77% |
| Дублирование кода | ~40% | < 10% | -75% |
| Документация | 0% | 80%+ | +80% |
| Производительность | Базовый | Улучшен | Устранены клонирования, оптимизированы алгоритмы |

## Риски и меры

1. **Риск**: Рефакторинг может сломать существующую функциональность
   - **Мера**: TDD подход, тесты перед рефакторингом
2. **Риск**: Изменение API может сломать зависимости
   - **Мера**: Постепенный рефакторинг, сохранение обратной совместимости
3. **Риск**: Оптимизации могут ухудшить читаемость
   - **Мера**: Баланс между производительностью и читаемостью
