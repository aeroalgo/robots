# 🚀 Быстрый старт - Тестирование и Деплой

## Минимальная инструкция

### 1️⃣ Запуск всего (тесты + production):

```bash
cd docker/test
./run-tests-then-deploy.sh
```

**Что происходит:**
1. ✅ Поднимается тестовое окружение (ClickHouse, MongoDB, Redis)
2. ✅ Применяются миграции
3. ✅ Запускаются 24 интеграционных теста
4. ✅ Если тесты прошли → запускается production
5. ❌ Если тесты провалились → деплой отменяется

---

### 2️⃣ Только тесты (без деплоя):

```bash
cd docker/test
make test
```

---

### 3️⃣ Посмотреть статус:

```bash
cd docker/test
make status
```

---

## 📦 Что тестируется?

### ClickHouse (12 тестов)
- OHLCV данные (свечи)
- Тиковые данные
- Символы
- Индикаторы
- Сигналы
- Сделки
- Стратегии
- Результаты бэктестов
- Batch операции
- Аналитика

### MongoDB (12 тестов)
- Конфигурации
- Метаданные
- Настройки пользователей
- Системные конфигурации
- Агрегации
- Поиск
- Bulk операции
- Performance

---

## 🔧 Другие полезные команды

```bash
cd docker/test

make help           # Все доступные команды
make logs           # Показать логи
make logs-ch        # Логи ClickHouse
make logs-mongo     # Логи MongoDB
make down           # Остановить тесты
make clean          # Полная очистка
```

---

## 🐛 Если тесты не проходят

### 1. Проверьте логи:
```bash
cd docker/test
make logs
```

### 2. Проверьте статус БД:
```bash
make status
docker ps | grep test
```

### 3. Подключитесь к БД:
```bash
# ClickHouse
make shell-ch

# MongoDB
make shell-mongo
```

### 4. Перезапустите с очисткой:
```bash
make clean
./run-tests-then-deploy.sh
```

---

## 📚 Полная документация

- `docker/test/README.md` - Полная документация тестов
- `docker/test/TESTS_SUMMARY.md` - Итоговый отчет
- `CLICKHOUSE_REFACTORING.md` - Изменения в ClickHouse
- `INTEGRATION_TESTS_COMPLETE.md` - Полный отчет

---

## ⚡ Требования

- Docker
- Docker Compose
- ~2GB свободной памяти
- ~5GB свободного места на диске

---

**Время выполнения**: ~3 минуты (с поднятием БД)  
**Автоматизация**: 100%  
**Изоляция**: 100%




























