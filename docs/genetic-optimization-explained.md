# Как работает генетическая оптимизация

## Общий процесс

Генетический алгоритм оптимизирует **параметры стратегий**. Для каждой **структуры стратегии** (комбинация индикаторов, условий, стопов) генерируются и тестируются различные **варианты параметров**.

## Этапы оптимизации

### 1. Генерация начальной популяции

**Что происходит:**
- Strategy Discovery генерирует различные **структуры стратегий** (StrategyCandidate)
  - Разные комбинации индикаторов
  - Разные условия входа/выхода
  - Разные стоп-обработчики
  - Разные таймфреймы

**Сколько стратегий:**
- Генерируется `population_size × decimation_coefficient` стратегий
- Например: 30 × 2.0 = 60 стратегий
- Затем фильтруются по пороговым значениям
- Остается `population_size` стратегий (например, 30)

**Для каждой стратегии:**
- Извлекаются параметры по умолчанию (из диапазонов оптимизации)
- Выполняется backtest с этими параметрами
- Рассчитывается fitness score
- Если не проходит пороги - отбрасывается

### 2. Эволюция поколений

**Для каждого поколения:**

1. **Селекция** - выбор лучших стратегий (родителей)
   - Используется roulette wheel selection
   - Вероятность выбора пропорциональна fitness

2. **Скрещивание (Crossover)** - создание новых вариантов параметров
   - Берется структура стратегии от одного родителя
   - Параметры комбинируются от обоих родителей
   - Вероятность скрещивания: `crossover_rate` (обычно 0.7)

3. **Мутация** - случайное изменение параметров
   - Параметры немного изменяются в пределах диапазона
   - Вероятность мутации: `mutation_rate` (обычно 0.1)

4. **Оценка** - тестирование новых вариантов
   - Для каждого нового варианта параметров выполняется backtest
   - Рассчитывается fitness score

5. **Элитизм** - сохранение лучших
   - `elitism_count` лучших стратегий сохраняются в следующем поколении

6. **Замена** - удаление слабых
   - Слабые стратегии заменяются новыми (из скрещивания/мутации)

### 3. Миграция между островами (если islands_count > 1)

**Каждые `migration_interval` поколений:**
- Лучшие стратегии мигрируют между островами
- Это помогает избежать локальных оптимумов
- Процент миграции: `migration_rate` (обычно 1-5%)

### 4. Инъекция "свежей крови"

**Периодически:**
- Обнаруживаются дубликаты стратегий
- Слабые стратегии заменяются новыми (сгенерированными)
- Процент замены: `fresh_blood_rate` (обычно 10%)

## Сколько вариантов тестируется?

**Формула:**
```
Всего вариантов ≈ population_size × max_generations
```

**Пример:**
- `population_size = 30`
- `max_generations = 5`
- **Итого: ~150 вариантов стратегий**

**Но важно понимать:**
- Каждая стратегия может иметь разные параметры
- В каждом поколении генерируются новые варианты параметров
- Некоторые стратегии могут тестироваться несколько раз с разными параметрами

## Что вы увидите при запуске

### Во время оптимизации:

```
Поколение 1/5
🏝️  Остров 0 (поколение 0)
   Лучший fitness: 0.5234
   Total Profit: 1250.50
   Sharpe Ratio: 1.25
   Win Rate: 52.3%
   Trades: 45
   Стратегий в популяции: 30
   Стратегий с fitness: 28 (прошли пороги)
```

### После завершения:

```
🏆 Лучшие стратегии по островам:

═══════════════════════════════════════════════════════
Остров 0 - Топ 5 стратегий:
═══════════════════════════════════════════════════════

📍 Место 1:
   Fitness: 0.6234

   📊 Структура стратегии:
      Индикаторы (2):
        - SMA (sma)
        - EMA (ema)
      Условия входа (1):
        - SMA crosses above EMA
      Стоп-обработчики (2):
        - Stop Loss
        - Take Profit
      Таймфреймы: [Minutes(60), Minutes(120)]

   ⚙️  Оптимизированные параметры:
      sma_period = 150
      ema_period = 120
      stop_loss_size = 2.5
      take_profit_size = 5.0

   💰 Результаты backtest:
      Total Profit: 1850.25
      Sharpe Ratio: 1.45
      Profit Factor: 2.10
      CAGR: 18.50%
      Win Rate: 55.2%
      Drawdown: -450.30
      Drawdown %: 12.5%
      Trades: 52
      Expectancy: 35.58
```

## Важные моменты

1. **Структура стратегии** (индикаторы, условия) определяется Strategy Discovery
2. **Параметры стратегии** (периоды, размеры стопов) оптимизируются генетическим алгоритмом
3. **Каждая стратегия** тестируется с конкретными параметрами
4. **Лучшие комбинации** сохраняются и улучшаются в следующих поколениях

## Пример расчета

Если у вас:
- `max_optimization_params = 8`
- Это может быть: 3 индикатора по 2 параметра (6) + стоп-лосс (1) + тейк-профит (1) = 8 параметров

Для каждого параметра есть диапазон оптимизации:
- Период SMA: 100-200 (шаг 10) = 11 вариантов
- Период EMA: 100-200 (шаг 10) = 11 вариантов
- И т.д.

**Теоретически** комбинаций может быть очень много, но генетический алгоритм:
- Не перебирает все варианты
- Фокусируется на лучших областях
- Эволюционирует к оптимальным значениям

